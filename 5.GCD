class Solution {
    public static int gcd(int a, int b) {
    //if b ==0 then a will be GCD
    
         if(b==0)
            return a;
        else
    //if b is not zero then b will be divident and a%b will be divisor
            return gcd(b,a%b);
    
    }
}
        
